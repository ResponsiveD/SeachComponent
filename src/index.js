import React from "react";
import ReactDOM from "react-dom";
import SearchComponent from "./SearchComponent";
import "./styles.css";

const rootElement = document.getElementById("root");
ReactDOM.render(<SearchComponent />, rootElement);
